# Secret key used to sign and verify JWT authentication tokens.
# Set this to a long, random string in any real deployment.
# If left unset, the application falls back to an insecure default
# and prints a warning to the logs — this is fine for quick local
# testing but must never be relied on in production.
JWT_SECRET=your-random-secret-key-here